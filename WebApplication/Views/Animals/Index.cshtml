@model IEnumerable<Animal>
@{
    ViewData["Title"] = "Animales";
}

<div class="page-header">
    <h1>Animales</h1>
    <a class="primary-btn" asp-action="Create">Nuevo animal</a>
</div>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Especie</th>
            <th>Nombre popular</th>
            <th>Jaula</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Especie</td>
            <td>@item.NombrePopular</td>
            <td>@item.Jaula?.Codigo</td>
            <td class="actions">
                <a asp-action="Details" asp-route-id="@item.Id">Detalle</a>
                <a asp-action="Edit" asp-route-id="@item.Id">Editar</a>
                <a asp-action="Delete" asp-route-id="@item.Id">Borrar</a>
            </td>
        </tr>
    }
    </tbody>
</table>
